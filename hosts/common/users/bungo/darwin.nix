# Darwin-specific user configuration for bungo
{
  config,
  lib,
  pkgs,
  ...
}:
{
  # Darwin-specific user settings
  users.users.bungo = {
    # Darwin-specific configuration would go here
  };
}
