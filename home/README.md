## home
This directory is for user level configurations. Each user gets their own directory.
And each users directory need to contain directories for the host they are on (this allows for user level configuation per host).
